  <style>
    /* Full‑page, click‑through canvas that sits on top of your site */
    #autumn-leaves {
      position: fixed;
      inset: 0;
      width: 100vw;
      height: 100vh;
      z-index: 99999;
      pointer-events: none; /* so it never blocks clicks */
      display: block;
    }
    /* Optional: graceful fallback if user prefers reduced motion */
    @media (prefers-reduced-motion: reduce) {
      #autumn-leaves { opacity: 0.6; }
    }
  </style>
