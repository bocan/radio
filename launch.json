{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start Local Server",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "start-server"
        }
    ]
}